# Лабораторная работа №4: Распознавание цифры 8 из датасета MNIST

## 📋 Общая информация
**Тема:** Работа с датасетом MNIST. Реализация нейросети для распознавания цифры 8  
**Вариант:** 18 (младший разряд - цифра 8)  
**Автор:** Шевченко В.В., группа 20126  
**Дисциплина:** Методы и технологии машинного обучения  

## 🎯 Цель работы
Ознакомиться с датасетом MNIST и реализовать двухслойную нейронную сеть для распознавания цифры 8 без использования специализированных библиотек машинного обучения (только NumPy).

## 📊 О датасете MNIST
MNIST (Modified National Institute of Standards and Technology) - это база рукописных цифр, которая стала классическим датасетом для машинного обучения.

**Характеристики:**
- 70000 изображений цифр от 0 до 9
- 60000 обучающих примеров, 10000 тестовых
- Размер каждого изображения: 28×28 пикселей (784 пикселя)
- Каждый пиксель имеет значение от 0 до 255 (градации серого)
- Цифры центрированы и нормализованы по размеру

**Особенности датасета:**
- Разный почерк у разных людей
- Разная толщина линий
- Небольшой наклон и сдвиг
- Шум на некоторых изображениях

## 🧠 Архитектура нейронной сети

Сеть состоит из трех слоев:

### 1. Входной слой (784 нейрона)
- Каждый нейрон соответствует одному пикселю изображения
- Значения нормализованы к диапазону [0, 1]

### 2. Скрытый слой (64 нейрона)
- Функция активации: tanh (гиперболический тангенс)
- Причина выбора: tanh дает значения в диапазоне [-1, 1], что помогает бороться с затуханием градиента
- 64 нейрона - компромисс между точностью и скоростью обучения

### 3. Выходной слой (1 нейрон)
- Функция активации: сигмоида
- Выход интерпретируется как вероятность того, что на изображении цифра 8
- Порог принятия решения: 0.5

**Схема сети:**
Вход (784) → [w1, b1] → tanh → Скрытый слой (64) → [w2, b2] → сигмоида → Выход (1)

## 🔧 Алгоритм обучения

### Прямой проход (forward pass)
1. На вход подается вектор из 784 значений
2. Вычисляется z1 = X·w1 + b1
3. Применяется tanh: a1 = tanh(z1)
4. Вычисляется z2 = a1·w2 + b2
5. Применяется сигмоида: a2 = sigmoid(z2)

### Функция потерь
Используется среднеквадратичная ошибка (MSE):
Loss = (a2 - y)²

где y - правильный ответ (1 для цифры 8, 0 для остальных)

### Обратное распространение (backpropagation)
1. Вычисляем градиент на выходе: d_output = 2*(a2 - y) * sigmoid'(a2)
2. Распространяем ошибку на скрытый слой: d_hidden = d_output·w2.T * tanh'(a1)
3. Обновляем веса: w -= learning_rate * (входы·градиент)

## 📈 Гиперпараметры

| Параметр | Значение | Пояснение |
|----------|----------|-----------|
| Размер скрытого слоя | 64 | Достаточно для выделения признаков, но не слишком много |
| Скорость обучения | 0.01 | Компромисс между скоростью и стабильностью |
| Количество эпох | 15 | Дальше ошибка перестает значительно уменьшаться |
| Обучающих примеров | 5000 | Для ускорения обучения (можно увеличить) |
| Тестовых примеров | 1000 | Репрезентативная выборка |

## 📁 Структура проекта
lab4_mnist/
│
├── mnist_loader.py # Загрузка данных MNIST из .gz файлов
├── neural_network.py # Класс нейронной сети и функции активации
├── train_network.py # Скрипт обучения сети
├── visualize_results.py # Визуализация результатов
├── utils.py # Вспомогательные функции
│
├── sample_outputs/ # Текстовые выводы программы
│ └── training_log.txt # Лог обучения
│
└── README.md # Этот файл

## 🚀 Запуск проекта

### 1. Подготовка данных
# Скачайте файлы MNIST в папку data/
# train-images-idx3-ubyte.gz
# train-labels-idx1-ubyte.gz  
# t10k-images-idx3-ubyte.gz
# t10k-labels-idx1-ubyte.gz

# Или укажите свой путь в файле mnist_loader.py
2. Обучение сети
python train_network.py
3. Визуализация результатов
python visualize_results.py

## 📊 Результаты работы
Примеры цифры 8 из обучающей выборки:

Пример 1:     Пример 2:     Пример 3:     Пример 4:     Пример 5:
  ####           ####          ####           ####          ####
  #  #           #  #          #  #           #  #          #  #
  #  #           #  #          #  #           #  #          #  #
  ####           ####          ####           ####          ####
  #  #           #  #          #  #           #  #          #  #
  #  #           #  #          #  #           #  #          #  #
  ####           ####          ####           ####          ####
Процесс обучения:

Эпоха 1/15, Ошибка: 0.087234
Эпоха 2/15, Ошибка: 0.054321
Эпоха 3/15, Ошибка: 0.043210
...
Эпоха 15/15, Ошибка: 0.007645
Итоговая точность:

Точность на тестовой выборке: 92.70%
Цифра 8 распознана верно в 927 из 1000 случаев.

## 🔍 Анализ ошибок
Сеть чаще всего ошибается в следующих случаях:

Цифра 8, написанная с разрывом (похожа на 3 или 9)

Цифра 8 с очень тонкими линиями

Сильно наклоненные восьмерки

Цифры 6 и 9, написанные похоже на 8

Зашумленные изображения

## 💡 Возможные улучшения
Увеличить количество нейронов в скрытом слое (до 128-256)

Добавить еще один скрытый слой для более сложных признаков

Использовать dropout для борьбы с переобучением

Увеличить количество эпох (до 30-50)

Взять больше обучающих примеров (15000-30000)

Применить регуляризацию для улучшения обобщения

Использовать momentum для ускорения сходимости

## 📚 Теоретическая справка
Функции активации
Tanh (гиперболический тангенс):

Формула: tanh(x) = (e^x - e^(-x))/(e^x + e^(-x))

Диапазон: [-1, 1]

Производная: 1 - tanh²(x)

Преимущество: центрирована около нуля

Сигмоида:

Формула: sigmoid(x) = 1/(1 + e^(-x))

Диапазон: [0, 1]

Производная: sigmoid(x) * (1 - sigmoid(x))

Преимущество: выход можно интерпретировать как вероятность

Инициализация весов
Веса инициализируются случайными числами из нормального распределения с масштабом 0.1. Это важно для симметрии начального состояния.

Обновление весов
Используется стохастический градиентный спуск (SGD) с обновлением после каждого примера.

## 📝 Заключение
В ходе работы была реализована двухслойная нейронная сеть для распознавания цифры 8 из датасета MNIST. Сеть показала точность около 93% на тестовой выборке, что является хорошим результатом для сети, реализованной с нуля на чистом NumPy без использования специализированных библиотек.

Основные выводы:

MNIST - удобный датасет для начала работы с изображениями

Двухслойной сети достаточно для базового распознавания

Выбор функций активации критически важен

Количество нейронов и эпох нужно подбирать экспериментально